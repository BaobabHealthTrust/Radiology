<script src="/javascripts/standard.js" type="text/javascript"></script>
<script src="/javascripts/touchscreenToolkit.js" type="text/javascript"></script>
<style type="text/css">

  .dateselector {
     position: fixed;
     top: 345px;
  }

  .tt_controls_summary #clearButton { display: none; }

  .tt_controls_summary .inputFrameClass { height: 448px; }

  #tt_page_summary .inputFrameClass { height: 448px; }

  .selected_conditions {
  font-size: 25px;
  padding-left: 5px;
  font-weight: bold;
  }
  #tt_page_select_year .inputFrameClass{
    height: 581px;   
  }
  #tt_page_select_year .inputFrameClass .options{
    height: 570px;
  }
  #tt_page_select_month .inputFrameClass{
    height: 581px;
  }
  #tt_page_select_month .inputFrameClass .options{
    height: 570px;
  }
</style>


<% form_tag "/report/print_investigation", :method => :post do %>

    <%= hidden_field_tag "id" , params[:id] %>
   
   <% @investigation_options = ['Xray','Ultrasound','MRI scan','Computed tomography scan','Bone densitometry','Mammography'].sort %>
   <% @months = [['January','1'],['February','2'],['March','3'],['April','4'],
                 ['May','5'],['June','6'],['July','7'],['August','8'],
                 ['September','9'],['October','10'],['November','11'],['December','12']]%>
   <% @years = [''] %>
   <% Date.today.year.downto(2004) do |year| @years << year.to_s
    end%>
   
     <%=  select_tag :year, options_for_select(@years),
     {:helpText => "Select year", :tt_onLoad => "document.getElementById('keyboard').style.display='none'"} %>
    <%=  select_tag :month, options_for_select(@months),
     {:helpText => "Select month"} %>
   <%=  select_tag :investigation_type, options_for_select(@investigation_options),
     {:helpText => "Select type of investigation"} %>
  
<%end%>
